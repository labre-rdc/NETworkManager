<UserControl x:Class="NETworkManager.Views.FirewallView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:NETworkManager.Converters;assembly=NETworkManager.Converters"
             xmlns:wpfHelpers="clr-namespace:NETworkManager.Utilities.WPF;assembly=NETworkManager.Utilities.WPF"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:networkManager="clr-namespace:NETworkManager"
             xmlns:mahAppsControls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:validators="clr-namespace:NETworkManager.Validators;assembly=NETworkManager.Validators"
             xmlns:profiles="clr-namespace:NETworkManager.Profiles;assembly=NETworkManager.Profiles"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:viewModels="clr-namespace:NETworkManager.ViewModels"
             xmlns:network="clr-namespace:NETworkManager.Models.Network;assembly=NETworkManager.Models"
             xmlns:firewall="clr-namespace:NETworkManager.Models.Firewall;assembly=NETworkManager.Models"
             xmlns:localization="clr-namespace:NETworkManager.Localization.Resources;assembly=NETworkManager.Localization"
             xmlns:settings="clr-namespace:NETworkManager.Settings;assembly=NETworkManager.Settings"
             xmlns:controls="clr-namespace:NETworkManager.Controls;assembly=NETworkManager.Controls"
             xmlns:localizationid="clr-namespace:NETworkManager.Localization;assembly=NETworkManager.Localization"
             xmlns:models="clr-namespace:NETworkManager.Models;assembly=NETworkManager.Models"
             xmlns:dialogs="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
             xmlns:sys="clr-namespace:System;assembly=System.Runtime"
             dialogs:DialogParticipation.Register="{Binding}"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance viewModels:FirewallViewModel}">
    <UserControl.Resources>
        <converters:BooleanReverseConverter x:Key="BooleanReverseConverter" />
        <converters:BooleanToEnumConverter x:Key="BooleanToEnumConverter" />
        <converters:BooleanReverseToVisibilityCollapsedConverter x:Key="BooleanReverseToVisibilityCollapsedConverter" />
        <converters:BooleanToStringConverter x:Key="BooleanToStringConverter" />
        <converters:BooleanToVisibilityCollapsedConverter x:Key="BooleanToVisibilityCollapsedConverter" />
        <converters:BooleansOrToVisibilityCollapsedConverter x:Key="BooleansOrToVisibilityCollapsedConverter" />
        <converters:IntNotZeroToVisibilityCollapsedConverter x:Key="IntNotZeroToVisibilityCollapsedConverter" />
        <converters:IntToStringConverter x:Key="IntToStringConverter" />
        <converters:IntZeroToVisibilityCollapsedConverter x:Key="IntZeroToVisibilityCollapsedConverter" />
        <converters:StringIsNotNullOrEmptyToBooleanConverter x:Key="StringIsNotNullOrEmptyToBooleanConverter" />
        <converters:StringNullOrEmptyToStringConverter x:Key="StringNullOrEmptyToStringConverter" />
        <converters:EnumToStringConverter x:Key="EnumToStringConverter" />
        <converters:BoolArrayToFwRuleCategoriesConverter x:Key="BoolArrayToFwRuleCategoriesConverter" />
        <converters:EnumToIntConverter x:Key="EnumToIntConverter"/>
        <wpfHelpers:BindingProxy x:Key="BindingProxy" Data="{Binding}" />
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition MinWidth="{x:Static settings:GlobalStaticConfiguration.Profile_WidthCollapsed}"
                              Width="{Binding ProfileWidth, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                              MaxWidth="{x:Static settings:GlobalStaticConfiguration.Profile_MaxWidthExpanded}" />
        </Grid.ColumnDefinitions>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="8*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>
            <TextBlock Style="{StaticResource ResourceKey=HeaderTextBlock}"
                       Text="{x:Static Member=localization:Strings.FirewallRules}" />
            <controls:MultiSelectDataGrid Grid.Column="0" Grid.Row="1"
                                          ItemsSource="{Binding Path=FirewallRules, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          SelectedItem="{Binding Path=SelectedRule, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          SelectedItemsList="{Binding Path=SelectedRules, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          RowDetailsVisibilityMode="Collapsed"
                                          EnableRowVirtualization="True"
                                          VerticalScrollBarVisibility="Auto"
                                          ScrollViewer.CanContentScroll="True"
                                          VirtualizingPanel.IsVirtualizing="True"
                                          VirtualizingPanel.IsVirtualizingWhenGrouping="True"
                                          VirtualizingPanel.IsContainerVirtualizable="True"
                                          VirtualizingPanel.VirtualizationMode="Recycling"
                                          VirtualizingPanel.ScrollUnit="Item">
                <controls:MultiSelectDataGrid.Resources>
                    <ObjectDataProvider x:Key="ProtocolNames" MethodName="GetEnumTranslation"
                                        ObjectType="{x:Type viewModels:FirewallRuleViewModel}">
                        <ObjectDataProvider.MethodParameters>
                            <x:Type TypeName="firewall:FirewallProtocol"/>
                        </ObjectDataProvider.MethodParameters>
                    </ObjectDataProvider>
                    <ObjectDataProvider x:Key="DirectionNames" MethodName="GetEnumTranslation"
                                        ObjectType="{x:Type viewModels:FirewallRuleViewModel}">
                        <ObjectDataProvider.MethodParameters>
                            <x:Type TypeName="firewall:FirewallRuleDirection"/>
                        </ObjectDataProvider.MethodParameters>
                    </ObjectDataProvider>
                    <wpfHelpers:BindingProxy x:Key="BindingProxy" Data="{Binding}" />
                    <ContextMenu x:Key="MultiRowContextMenu" Opened="ContextMenu_Opened" MinWidth="150">
                        <MenuItem Header="{x:Static localization:Strings.Add}"
                                  Command="{Binding AddRuleCommand}">
                            <MenuItem.Icon>
                                <Rectangle Width="16" Height="16" Fill="{DynamicResource MahApps.Brushes.Gray3}">
                                    <Rectangle.OpacityMask>
                                        <VisualBrush Stretch="Uniform" Visual="{iconPacks:Modern Kind=Add}" />
                                    </Rectangle.OpacityMask>
                                </Rectangle>
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="{x:Static localization:Strings.Delete}"
                                  Command="{Binding DeleteRulesCommand}">
                            <MenuItem.Icon>
                                <Rectangle Width="16" Height="16" Fill="{DynamicResource MahApps.Brushes.Gray3}">
                                    <Rectangle.OpacityMask>
                                        <VisualBrush Stretch="Uniform" Visual="{iconPacks:Modern Kind=Delete}" />
                                    </Rectangle.OpacityMask>
                                </Rectangle>
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="{x:Static localization:Strings.Apply}"
                                  Command="{Binding ApplyConfigurationCommand}">
                            <MenuItem.Icon>
                                <Rectangle Width="16" Height="16" Fill="{DynamicResource MahApps.Brushes.Gray3}">
                                    <Rectangle.OpacityMask>
                                        <VisualBrush Stretch="Uniform" Visual="{iconPacks:Modern Kind=Check}" />
                                    </Rectangle.OpacityMask>
                                </Rectangle>
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </controls:MultiSelectDataGrid.Resources>
                <controls:MultiSelectDataGrid.RowStyle>
                    <Style TargetType="{x:Type DataGridRow}" BasedOn="{StaticResource MahApps.Styles.DataGridRow}">
                        <Setter Property="ContextMenu" Value="{StaticResource MultiRowContextMenu}" />
                    </Style>
                </controls:MultiSelectDataGrid.RowStyle>
                <controls:MultiSelectDataGrid.Columns>
                    <DataGridTemplateColumn Header="" Width="24" CanUserResize="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Click="ButtonBase_OnClick" Style="{StaticResource CleanButton}">
                                    <Rectangle x:Name="ExpandRowDetails" Height="16" Width="16"
                                               Fill="{DynamicResource MahApps.Brushes.Gray3}">
                                        <Rectangle.OpacityMask>
                                            <VisualBrush Stretch="Uniform" Visual="{iconPacks:Material Kind=ChevronUp}" />
                                        </Rectangle.OpacityMask>
                                    </Rectangle>
                                </Button>
                                <DataTemplate.Triggers>
                                    <DataTrigger
                                        Binding="{Binding Path=DetailsVisibility, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=DataGridRow}}"
                                        Value="Visible">
                                        <Setter TargetName="ExpandRowDetails" Property="OpacityMask">
                                            <Setter.Value>
                                                <VisualBrush Stretch="Uniform"
                                                             Visual="{iconPacks:Material Kind=ChevronDown}" />
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ExpandRowDetails" Property="Fill"
                                                Value="{DynamicResource ResourceKey=MahApps.Brushes.Gray5}" />
                                    </Trigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="{x:Static localization:Strings.Name}"
                                        Binding="{Binding Name}"
                                        SortMemberPath="Name"
                                        MinWidth="150" />
                    <DataGridTextColumn Header="{x:Static localization:Strings.Protocol}"
                                        Binding="{Binding Protocol,
                                        Converter={StaticResource EnumToStringConverter}}"
                                        SortMemberPath="Protocol"
                                        MinWidth="150">
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="{x:Static localization:Strings.Direction}"
                                        Binding="{Binding Direction,
                                        Converter={StaticResource EnumToStringConverter}}"
                                        SortMemberPath="Direction"
                                        MinWidth="150" />
                    <DataGridTextColumn Header="{x:Static localization:Strings.LocalPorts}"
                                        Binding="{Binding LocalPorts}"
                                        Visibility="{Binding Path=(viewModels:FirewallRuleViewModel.LocalPorts).Count,
                                        Converter={StaticResource IntNotZeroToVisibilityCollapsedConverter}}"
                                        SortMemberPath="LocalPorts"
                                        MinWidth="150" />
                    <DataGridTextColumn Header="{x:Static localization:Strings.RemotePorts}"
                                        Binding="{Binding RemotePorts}"
                                        SortMemberPath="RemotePorts"
                                        MinWidth="150" />
                    <DataGridTextColumn Header="{x:Static localization:Strings.Program}"
                                        Binding="{Binding Program.Executable}"
                                        SortMemberPath="Program"
                                        MinWidth="150" />
                    <DataGridTextColumn Header="{x:Static localization:Strings.NetworkCategory}"
                                        Binding="{Binding NetworkCategory,
                                        Converter={StaticResource BoolArrayToFwRuleCategoriesConverter}}"
                                        SortMemberPath="NetworkCategory"
                                        MinWidth="150" />
                    <DataGridTextColumn Header="{x:Static localization:Strings.Action}"
                                        Binding="{Binding Action,
                                        Converter={StaticResource EnumToStringConverter}}"
                                        SortMemberPath="Action"
                                        MinWidth="150" />
                </controls:MultiSelectDataGrid.Columns>
                <controls:MultiSelectDataGrid.RowDetailsTemplate>
                    <DataTemplate DataType="{x:Type viewModels:FirewallRuleViewModel}">
                        <!-- Highlight the border when the row is selected -->
                        <Border BorderThickness="1,0,1,1"
                                Background="{DynamicResource MahApps.Brushes.Window.Background}">
                            <Border.Style>
                                <Style TargetType="{x:Type TypeName=Border}">
                                    <Style.Triggers>
                                        <DataTrigger
                                            Binding="{Binding Path=IsSelected, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=DataGridRow}}"
                                            Value="False">
                                            <Setter Property="BorderBrush"
                                                    Value="{DynamicResource MahApps.Brushes.Gray8}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid Margin="20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="10" />
                                    <ColumnDefinition Width="4*" />
                                    <ColumnDefinition Width="10" />
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="10" />
                                    <ColumnDefinition Width="2*" />
                                </Grid.ColumnDefinitions>
                                <Grid.Resources>
                                    <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource CenterTextBlock}">
                                        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Gray5}" />
                                    </Style>
                                    
                                </Grid.Resources>
                                <!-- User defined name -->
                                <Grid Grid.Row="0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="10" />
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="10" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="10" />
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="10" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="10" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Column="0" Grid.Row="0"
                                               Text="{x:Static localization:Strings.Name}"
                                               Style="{StaticResource BoldTextBlock}" />
                                    <TextBox x:Name="TextBoxRuleUserName" Grid.Column="0" Grid.Row="2"
                                             mah:TextBoxHelper.Watermark="{Binding DefaultName}"
                                             MaxLength="9999">
                                        <TextBox.Text>
                                            <Binding Path="UserDefinedName" Mode="TwoWay"
                                                     UpdateSourceTrigger="PropertyChanged">
                                                <Binding.ValidationRules>
                                                    <validators:FirewallRuleNameDescValidator />
                                                </Binding.ValidationRules>
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                    <!-- Protocol -->
                                    <TextBlock Grid.Row="4"
                                               Text="{x:Static localization:Strings.Protocol}"
                                               Style="{StaticResource BoldTextBlock}" />
                                    <ComboBox x:Name="ComboBoxProtocol" Grid.Row="6"
                                              Style="{StaticResource DefaultComboBox}"
                                              ItemsSource="{Binding
                                              Source={StaticResource ProtocolNames}}"
                                              SelectedIndex="{Binding Protocol,
                                              Converter={StaticResource EnumToIntConverter}}"
                                              SelectedItem="{Binding Protocol,
                                              Mode=TwoWay,
                                              UpdateSourceTrigger=PropertyChanged}"/>
                                    <!-- Direction -->
                                    <TextBlock Grid.Row="8"
                                               Text="{x:Static localization:Strings.Direction}"
                                               Style="{StaticResource BoldTextBlock}" />
                                    <ComboBox Grid.Row="10"
                                              Style="{StaticResource DefaultComboBox}"
                                              ItemsSource="{Binding
                                              Source={StaticResource DirectionNames}}"
                                              SelectedIndex="{Binding Direction,
                                              Converter={StaticResource EnumToIntConverter}}"
                                              SelectedItem="{Binding Direction,
                                              UpdateSourceTrigger=PropertyChanged,
                                              Mode=TwoWay,
                                              Converter={StaticResource EnumToStringConverter}}"/>
                                </Grid>
                                <Grid Grid.Row="0" Grid.Column="2">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="10" />
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="10" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="10" />
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="10" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="10" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Column="0" Grid.Row="0"
                                               Text="TODO"
                                               Style="{StaticResource BoldTextBlock}" />
                                    </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </controls:MultiSelectDataGrid.RowDetailsTemplate>
            </controls:MultiSelectDataGrid>
            <StackPanel Grid.Row="1" Grid.Column="1" Margin="10, 0, 10, 0" MinWidth="150">
                <Button Margin="0,0,0,10" Command="{Binding AddRuleCommand}"
                        Style="{StaticResource ImageWithTextButton}">
                    <Button.Content>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Rectangle Style="{StaticResource ButtonWithImageRectangle}">
                                <Rectangle.OpacityMask>
                                    <VisualBrush Stretch="Uniform" Visual="{iconPacks:Modern Kind=Add}" />
                                </Rectangle.OpacityMask>
                            </Rectangle>
                            <TextBlock Grid.Column="1"
                                       Text="{x:Static localization:Strings.Add}"
                                       Style="{StaticResource ButtonWithImageTextBlock}" />
                        </Grid>
                    </Button.Content>
                </Button>
                <Button Margin="0,0,0,10"
                        Style="{StaticResource ImageWithTextButton}"
                        Command="{Binding DeleteRulesCommand}">
                    <Button.Content>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Rectangle Style="{StaticResource ButtonWithImageRectangle}">
                                <Rectangle.OpacityMask>
                                    <VisualBrush Stretch="Uniform" Visual="{iconPacks:Modern Kind=Delete}" />
                                </Rectangle.OpacityMask>
                            </Rectangle>
                            <TextBlock Grid.Column="1"
                                       Text="{x:Static localization:Strings.Delete}"
                                       Style="{StaticResource ButtonWithImageTextBlock}" />
                        </Grid>
                    </Button.Content>
                </Button>
                <Button Margin="0,0,0,10"
                        Style="{StaticResource ImageWithTextButton}"
                        Command="{Binding ApplyConfigurationCommand}">
                    <Button.Content>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Rectangle Style="{StaticResource ButtonWithImageRectangle}">
                                <Rectangle.OpacityMask>
                                    <VisualBrush Stretch="Uniform" Visual="{iconPacks:Modern Kind=Checkmark}" />
                                </Rectangle.OpacityMask>
                            </Rectangle>
                            <TextBlock Grid.Column="1"
                                       Text="{x:Static localization:Strings.Apply}"
                                       Style="{StaticResource ButtonWithImageTextBlock}" />
                        </Grid>
                    </Button.Content>
                </Button>
            </StackPanel>
        </Grid>
        <GridSplitter Grid.Column="1" Grid.Row="0"
                      Focusable="False"
                      Style="{StaticResource ProfileGridSplitter}" />
        <Expander Grid.Column="2" Grid.Row="0"
                  x:Name="ExpanderProfiles"
                  Header="{x:Static localization:Strings.Profiles}"
                  Style="{StaticResource ProfileExpander}"
                  IsExpanded="{Binding ExpandProfileView}">
            <Grid>
                <Grid
                    Visibility="{Binding Source={x:Static settings:ConfigurationManager.Current}, Path=ProfileManagerIsEnabled, Converter={StaticResource BooleanToVisibilityCollapsedConverter}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ToggleButton Grid.Column="0" Grid.Row="0"
                                      IsChecked="{Binding ExpandProfileView}"
                                      Focusable="False">
                            <ToggleButton.Style>
                                <Style TargetType="{x:Type ToggleButton}"
                                       BasedOn="{StaticResource MahApps.Styles.Button.Circle}">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type ToggleButton}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"
                                                                  RecognizesAccessKey="True" />
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ToggleButton.Style>
                            <Rectangle Width="16" Height="16">
                                <Rectangle.OpacityMask>
                                    <VisualBrush Stretch="Uniform" Visual="{iconPacks:Material Kind=ChevronRight}" />
                                </Rectangle.OpacityMask>
                                <Rectangle.Style>
                                    <Style TargetType="{x:Type Rectangle}">
                                        <Setter Property="Fill" Value="{DynamicResource MahApps.Brushes.Gray3}" />
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Fill"
                                                        Value="{DynamicResource MahApps.Brushes.Gray5}" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Rectangle.Style>
                            </Rectangle>
                        </ToggleButton>
                        <TextBox x:Name="TextBoxSearch" VerticalAlignment="Center" Grid.Column="2" Grid.Row="0"
                                 Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource SearchTextBox}" />
                        <Button Grid.Column="4" Grid.Row="0"
                                x:Name="ButtonProfileFilter"
                                Focusable="False"
                                Style="{StaticResource ResourceKey=CleanButton}"
                                Command="{Binding Path=OpenProfileFilterCommand}"
                                ToolTip="{x:Static Member=localization:Strings.FilterProfilesDots}">
                            <Rectangle Width="16" Height="16">
                                <Rectangle.Resources>
                                    <VisualBrush x:Key="VisualFilter" Stretch="Uniform"
                                                 Visual="{iconPacks:Material Kind=FilterOutline}" />
                                    <VisualBrush x:Key="VisualFilterRemove" Stretch="Uniform"
                                                 Visual="{iconPacks:Material Kind=FilterMultipleOutline}" />
                                </Rectangle.Resources>
                                <Rectangle.Style>
                                    <Style TargetType="{x:Type TypeName=Rectangle}">
                                        <Setter Property="OpacityMask"
                                                Value="{StaticResource ResourceKey=VisualFilter}" />
                                        <Setter Property="Fill"
                                                Value="{DynamicResource ResourceKey=MahApps.Brushes.Gray3}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=IsProfileFilterSet}" Value="True">
                                                <Setter Property="OpacityMask"
                                                        Value="{StaticResource ResourceKey=VisualFilterRemove}" />
                                            </DataTrigger>
                                            <DataTrigger
                                                Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type TypeName=Button}}, Path=IsMouseOver}"
                                                Value="True">
                                                <Setter Property="Fill"
                                                        Value="{DynamicResource ResourceKey=MahApps.Brushes.Gray5}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Rectangle.Style>
                            </Rectangle>
                        </Button>
                        <Popup PlacementTarget="{Binding ElementName=ButtonProfileFilter}"
                               Placement="Bottom" StaysOpen="False"
                               IsOpen="{Binding ProfileFilterIsOpen}"
                               Width="220">
                            <Border Background="{DynamicResource MahApps.Brushes.Window.Background}"
                                    BorderBrush="{DynamicResource MahApps.Brushes.Gray8}"
                                    BorderThickness="1">
                                <Grid Margin="10">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="10" />
                                        <RowDefinition MaxHeight="150" />
                                        <RowDefinition Height="10" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="10" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="10" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Column="0" Grid.Row="0"
                                               Text="{x:Static Member=localization:Strings.FilterByTags}"
                                               Style="{StaticResource InfoTextBlock}" />
                                    <ScrollViewer Grid.Column="0" Grid.Row="2"
                                                  VerticalScrollBarVisibility="Auto"
                                                  HorizontalScrollBarVisibility="Hidden"
                                                  Visibility="{Binding Path=ProfileFilterTagsView.Count, Converter={StaticResource IntNotZeroToVisibilityCollapsedConverter}}">
                                        <ItemsControl ItemsSource="{Binding ProfileFilterTagsView}"
                                                      Width="200">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate DataType="{x:Type profiles:ProfileFilterTagsInfo}">
                                                    <CheckBox Margin="0,5,10,5" Content="{Binding Name}"
                                                              IsChecked="{Binding IsSelected}"
                                                              Style="{StaticResource DefaultCheckBox}" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                    <TextBlock Grid.Column="0" Grid.Row="2"
                                               Text="{x:Static localization:Strings.NoTagsFound}"
                                               Visibility="{Binding Path=ProfileFilterTagsView.Count, Converter={StaticResource IntZeroToVisibilityCollapsedConverter}}"
                                               Style="{StaticResource MessageTextBlock}" />
                                    <TextBlock Grid.Column="0" Grid.Row="4"
                                               Text="{x:Static localization:Strings.Match}"
                                               Style="{StaticResource InfoTextBlock}" />
                                    <StackPanel Grid.Column="0" Grid.Row="6"
                                                Orientation="Horizontal">
                                        <RadioButton Content="{x:Static localization:Strings.Any}"
                                                     IsChecked="{Binding Path=ProfileFilterTagsMatchAny}" />
                                        <RadioButton Content="{x:Static localization:Strings.All}"
                                                     IsChecked="{Binding Path=ProfileFilterTagsMatchAll}"
                                                     Margin="10,0,0,0" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="0" Grid.Row="8"
                                                Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Button Command="{Binding ApplyProfileFilterCommand}"
                                                Style="{StaticResource ImageButton}"
                                                ToolTip="{x:Static localization:Strings.ApplyFilter}">
                                            <Rectangle Width="16" Height="16"
                                                       Fill="{DynamicResource MahApps.Brushes.Gray3}">
                                                <Rectangle.OpacityMask>
                                                    <VisualBrush Stretch="Uniform"
                                                                 Visual="{iconPacks:Material Kind=ContentSaveOutline}" />
                                                </Rectangle.OpacityMask>
                                            </Rectangle>
                                        </Button>
                                        <Button Command="{Binding ClearProfileFilterCommand}"
                                                Style="{StaticResource ImageButton}"
                                                ToolTip="{x:Static localization:Strings.ClearFilter}"
                                                Margin="10,0,0,0">
                                            <Rectangle Width="20" Height="20"
                                                       Fill="{DynamicResource MahApps.Brushes.Gray3}">
                                                <Rectangle.OpacityMask>
                                                    <VisualBrush Stretch="Uniform"
                                                                 Visual="{iconPacks:Material Kind=FilterRemoveOutline}" />
                                                </Rectangle.OpacityMask>
                                            </Rectangle>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Popup>
                        <Button Grid.Column="6" Grid.Row="0"
                                Focusable="False"
                                Style="{StaticResource CleanButton}"
                                Command="{Binding AddProfileCommand}"
                                ToolTip="{x:Static localization:Strings.AddProfileDots}">
                            <Rectangle Width="16" Height="16">
                                <Rectangle.OpacityMask>
                                    <VisualBrush Stretch="Uniform" Visual="{iconPacks:Material Kind=Plus}" />
                                </Rectangle.OpacityMask>
                                <Rectangle.Style>
                                    <Style TargetType="{x:Type Rectangle}">
                                        <Setter Property="Fill" Value="{DynamicResource MahApps.Brushes.Gray3}" />
                                        <Style.Triggers>
                                            <DataTrigger
                                                Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Button}}, Path=IsMouseOver}"
                                                Value="True">
                                                <Setter Property="Fill"
                                                        Value="{DynamicResource MahApps.Brushes.Gray5}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Rectangle.Style>
                            </Rectangle>
                        </Button>
                    </Grid>
                    <ListBox Grid.Column="0" Grid.Row="2" ItemsSource="{Binding Profiles}"
                             Visibility="{Binding IsSearching, Converter={StaticResource BooleanReverseToVisibilityCollapsedConverter}}"
                             SelectedItem="{Binding SelectedProfile}"
                             Style="{StaticResource ProfileListBox}"
                             ScrollViewer.CanContentScroll="True"
                             VirtualizingPanel.IsVirtualizing="True"
                             VirtualizingPanel.IsVirtualizingWhenGrouping="True"
                             VirtualizingPanel.IsContainerVirtualizable="True"
                             VirtualizingPanel.VirtualizationMode="Recycling"
                             VirtualizingPanel.ScrollUnit="Item">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel IsItemsHost="True" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.Resources>
                            <ContextMenu x:Key="ListBoxItemContextMenu" Opened="ContextMenu_Opened" MinWidth="150">
                                <MenuItem Header="{x:Static localization:Strings.Apply}"
                                          Command="{Binding ApplyProfileConfigCommand}">
                                    <MenuItem.Icon>
                                        <Rectangle Width="16" Height="16"
                                                   Fill="{DynamicResource MahApps.Brushes.Gray3}">
                                            <Rectangle.OpacityMask>
                                                <VisualBrush Stretch="Uniform"
                                                             Visual="{iconPacks:Material Kind=ContentSave}" />
                                            </Rectangle.OpacityMask>
                                        </Rectangle>
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="{x:Static localization:Strings.EditDots}"
                                          Command="{Binding EditProfileCommand}">
                                    <MenuItem.Icon>
                                        <Rectangle Width="16" Height="16"
                                                   Fill="{DynamicResource MahApps.Brushes.Gray3}">
                                            <Rectangle.OpacityMask>
                                                <VisualBrush Stretch="Uniform"
                                                             Visual="{iconPacks:Modern Kind=Edit}" />
                                            </Rectangle.OpacityMask>
                                        </Rectangle>
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="{x:Static localization:Strings.CopyAsDots}"
                                          Command="{Binding CopyAsProfileCommand}">
                                    <MenuItem.Icon>
                                        <Rectangle Width="16" Height="16"
                                                   Fill="{DynamicResource MahApps.Brushes.Gray3}">
                                            <Rectangle.OpacityMask>
                                                <VisualBrush Stretch="Uniform"
                                                             Visual="{iconPacks:Material Kind=ContentDuplicate}" />
                                            </Rectangle.OpacityMask>
                                        </Rectangle>
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="{x:Static localization:Strings.Delete}"
                                          Command="{Binding DeleteProfileCommand}">
                                    <MenuItem.Icon>
                                        <Rectangle Width="16" Height="16"
                                                   Fill="{DynamicResource MahApps.Brushes.Gray3}">
                                            <Rectangle.OpacityMask>
                                                <VisualBrush Stretch="Uniform"
                                                             Visual="{iconPacks:Material Kind=Close}" />
                                            </Rectangle.OpacityMask>
                                        </Rectangle>
                                    </MenuItem.Icon>
                                </MenuItem>
                            </ContextMenu>
                        </ListBox.Resources>
                        <ListBox.InputBindings>
                            <KeyBinding Command="{Binding EditProfileCommand}" Key="F2" />
                            <KeyBinding Command="{Binding DeleteProfileCommand}" Key="Delete" />
                        </ListBox.InputBindings>
                        <ListBox.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.ContainerStyle>
                                    <Style TargetType="{x:Type GroupItem}">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <controls:GroupExpander
                                                        Style="{StaticResource DefaultExpander}"
                                                        StateStore="{Binding Path=DataContext.GroupExpanderStateStore, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                        GroupName="{Binding Path=(CollectionViewGroup.Name)}">
                                                        <Expander.Header>
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="10" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>
                                                                <Grid.ContextMenu>
                                                                    <ContextMenu
                                                                        Style="{StaticResource DefaultContextMenu}">
                                                                        <MenuItem
                                                                            Header="{x:Static Member=localization:Strings.ExpandAll}"
                                                                            Command="{Binding Path=Data.(viewModels:FirewallViewModel.ExpandAllProfileGroupsCommand), Source={StaticResource BindingProxy}}">
                                                                            <MenuItem.Icon>
                                                                                <Rectangle Width="16" Height="16"
                                                                                    Fill="{DynamicResource ResourceKey=MahApps.Brushes.Gray3}">
                                                                                    <Rectangle.OpacityMask>
                                                                                        <VisualBrush
                                                                                            Stretch="Uniform"
                                                                                            Visual="{iconPacks:Material Kind=PlusBoxMultipleOutline}" />
                                                                                    </Rectangle.OpacityMask>
                                                                                </Rectangle>
                                                                            </MenuItem.Icon>
                                                                        </MenuItem>
                                                                        <MenuItem
                                                                            Header="{x:Static Member=localization:Strings.CollapseAll}"
                                                                            Command="{Binding Path=Data.(viewModels:FirewallViewModel.CollapseAllProfileGroupsCommand), Source={StaticResource BindingProxy}}">
                                                                            <MenuItem.Icon>
                                                                                <Rectangle Width="16" Height="16"
                                                                                    Fill="{DynamicResource ResourceKey=MahApps.Brushes.Gray3}">
                                                                                    <Rectangle.OpacityMask>
                                                                                        <VisualBrush
                                                                                            Stretch="Uniform"
                                                                                            Visual="{iconPacks:Material Kind=MinusBoxMultipleOutline}" />
                                                                                    </Rectangle.OpacityMask>
                                                                                </Rectangle>
                                                                            </MenuItem.Icon>
                                                                        </MenuItem>
                                                                    </ContextMenu>
                                                                </Grid.ContextMenu>
                                                                <Rectangle Grid.Column="0" Grid.ColumnSpan="3"
                                                                           Fill="Transparent" />
                                                                <TextBlock Grid.Column="0"
                                                                           Text="{Binding (CollectionViewGroup.Name)}"
                                                                           Style="{DynamicResource ProfileGroupTextBlock}" />
                                                                <Button Grid.Column="2"
                                                                        ToolTip="{x:Static localization:Strings.EditGroupDots}"
                                                                        Command="{Binding DataContext.EditGroupCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListBox}}}"
                                                                        CommandParameter="{Binding (CollectionViewGroup.Name)}"
                                                                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Grid}}, Converter={StaticResource BooleanToVisibilityCollapsedConverter}}"
                                                                        Style="{StaticResource CleanButton}">
                                                                    <Rectangle Width="16" Height="16">
                                                                        <Rectangle.OpacityMask>
                                                                            <VisualBrush Stretch="Uniform"
                                                                                    Visual="{iconPacks:Modern Kind=Edit}" />
                                                                        </Rectangle.OpacityMask>
                                                                        <Rectangle.Style>
                                                                            <Style TargetType="{x:Type Rectangle}">
                                                                                <Setter Property="Fill"
                                                                                    Value="{DynamicResource MahApps.Brushes.Gray3}" />
                                                                                <Style.Triggers>
                                                                                    <DataTrigger
                                                                                        Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Button}}, Path=IsMouseOver}"
                                                                                        Value="True">
                                                                                        <Setter Property="Fill"
                                                                                            Value="{DynamicResource MahApps.Brushes.Gray5}" />
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </Rectangle.Style>
                                                                    </Rectangle>
                                                                </Button>
                                                            </Grid>
                                                        </Expander.Header>
                                                        <ItemsPresenter />
                                                    </controls:GroupExpander>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </GroupStyle.ContainerStyle>
                            </GroupStyle>
                        </ListBox.GroupStyle>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="{x:Type ListBoxItem}"
                                   BasedOn="{StaticResource MahApps.Styles.ListBoxItem}">
                                <Setter Property="ContextMenu" Value="{StaticResource ListBoxItemContextMenu}" />
                                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type profiles:ProfileInfo}">
                                <Grid Background="Transparent">
                                    <Grid.ToolTip>
                                        <ToolTip MinWidth="250">
                                            <StackPanel Margin="10">
                                                <TextBlock Text="{Binding Host}"
                                                           Style="{StaticResource DefaultTextBlock}" />
                                                <Separator Background="{DynamicResource MahApps.Brushes.Gray8}"
                                                           Margin="0,10" />
                                                <TextBlock Text="{Binding Description}"
                                                           Style="{StaticResource InfoTextBlock}"
                                                           TextWrapping="Wrap" />
                                            </StackPanel>
                                        </ToolTip>
                                    </Grid.ToolTip>
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource CenterTextBlock}" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <mah:ProgressRing Grid.Row="2" Height="50" Width="50" IsActive="{Binding IsSearching}" />
                    <Grid Grid.Row="2" VerticalAlignment="Center">
                        <Grid.Style>
                            <Style TargetType="{x:Type Grid}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding Profiles.Count}" Value="0" />
                                            <Condition Binding="{Binding IsSearching}" Value="False" />
                                        </MultiDataTrigger.Conditions>
                                        <MultiDataTrigger.Setters>
                                            <Setter Property="Visibility" Value="Visible" />
                                        </MultiDataTrigger.Setters>
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Button HorizontalAlignment="Center" Focusable="False">
                            <Button.Style>
                                <Style TargetType="{x:Type Button}" BasedOn="{StaticResource CleanButton}">
                                    <Setter Property="Command" Value="{Binding AddProfileCommand}" />
                                    <Style.Triggers>
                                        <DataTrigger
                                            Binding="{Binding Path=IsProfileFilterSet}"
                                            Value="True">
                                            <Setter Property="Command" Value="{Binding Path=ClearProfileFilterCommand}" />
                                            <Setter Property="ToolTip"
                                                    Value="{x:Static localization:Strings.ClearFilter}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Rectangle Grid.Column="0" Width="20" Height="20">
                                    <Rectangle.Resources>
                                        <VisualBrush x:Key="VisualPlus" Stretch="Uniform"
                                                     Visual="{iconPacks:Material Kind=Plus}" />
                                        <VisualBrush x:Key="VisualClear" Stretch="Uniform"
                                                     Visual="{iconPacks:Material Kind=FilterRemoveOutline}" />
                                    </Rectangle.Resources>
                                    <Rectangle.Style>
                                        <Style TargetType="{x:Type Rectangle}">
                                            <Setter Property="OpacityMask" Value="{StaticResource VisualPlus}" />
                                            <Setter Property="Fill" Value="{DynamicResource MahApps.Brushes.Gray3}" />
                                            <Style.Triggers>
                                                <DataTrigger
                                                    Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Button}}, Path=IsMouseOver}"
                                                    Value="True">
                                                    <Setter Property="Fill"
                                                            Value="{DynamicResource MahApps.Brushes.Gray5}" />
                                                </DataTrigger>
                                                <DataTrigger
                                                    Binding="{Binding Path=IsProfileFilterSet}"
                                                    Value="True">
                                                    <Setter Property="OpacityMask"
                                                            Value="{StaticResource VisualClear}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Rectangle.Style>
                                </Rectangle>
                                <TextBlock Grid.Column="1">
                                    <TextBlock.Style>
                                        <Style TargetType="{x:Type TextBlock}"
                                               BasedOn="{StaticResource MessageTextBlock}">
                                            <Setter Property="Text"
                                                    Value="{x:Static localization:Strings.NoProfilesFoundCreateOne}" />
                                            <Style.Triggers>
                                                <DataTrigger
                                                    Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Button}}, Path=IsMouseOver}"
                                                    Value="True">
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource MahApps.Brushes.Gray5}" />
                                                </DataTrigger>
                                                <DataTrigger
                                                    Binding="{Binding Path=IsProfileFilterSet}"
                                                    Value="True">
                                                    <Setter Property="Text"
                                                            Value="{x:Static localization:Strings.NothingFound}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Button>
                    </Grid>
                </Grid>
                <Grid
                    Visibility="{Binding Source={x:Static settings:ConfigurationManager.Current}, Path=ProfileManagerIsEnabled, Converter={StaticResource BooleanReverseToVisibilityCollapsedConverter}}">
                    <Grid.RowDefinitions>
                        <!-- Adjust the Height without TextBox -->
                        <RowDefinition Height="27" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <ToggleButton Grid.Row="0" Grid.Column="0"
                                  IsChecked="{Binding ExpandProfileView}"
                                  HorizontalAlignment="Left"
                                  Focusable="False">
                        <ToggleButton.Style>
                            <Style TargetType="{x:Type ToggleButton}"
                                   BasedOn="{StaticResource MahApps.Styles.Button.Circle}">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"
                                                              RecognizesAccessKey="True" />
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ToggleButton.Style>
                        <Rectangle Width="16" Height="16">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Uniform" Visual="{iconPacks:Material Kind=ChevronRight}" />
                            </Rectangle.OpacityMask>
                            <Rectangle.Style>
                                <Style TargetType="{x:Type Rectangle}">
                                    <Setter Property="Fill" Value="{DynamicResource MahApps.Brushes.Gray3}" />
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Fill" Value="{DynamicResource MahApps.Brushes.Gray5}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Rectangle.Style>
                        </Rectangle>
                    </ToggleButton>
                    <Grid Grid.Column="0" Grid.Row="2">
                        <!-- Unlock profile button -->
                        <Button HorizontalAlignment="Center" VerticalAlignment="Center"
                                Focusable="False"
                                Command="{Binding Path=(networkManager:MainWindow.UnlockProfileCommand), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type TypeName=mah:MetroWindow}}}"
                                Style="{StaticResource CleanButton}"
                                Visibility="{Binding Source={x:Static Member=settings:ConfigurationManager.Current}, Path=ProfileManagerShowUnlock, Converter={StaticResource BooleanToVisibilityCollapsedConverter}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Rectangle Grid.Column="0" Grid.Row="0"
                                           Width="20" Height="20">
                                    <Rectangle.OpacityMask>
                                        <VisualBrush Stretch="Uniform"
                                                     Visual="{iconPacks:Modern Kind=InterfacePassword}" />
                                    </Rectangle.OpacityMask>
                                    <Rectangle.Style>
                                        <Style TargetType="{x:Type Rectangle}">
                                            <Setter Property="Fill" Value="{DynamicResource MahApps.Brushes.Gray3}" />
                                            <Style.Triggers>
                                                <DataTrigger
                                                    Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Button}}, Path=IsMouseOver}"
                                                    Value="True">
                                                    <Setter Property="Fill"
                                                            Value="{DynamicResource MahApps.Brushes.Gray5}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Rectangle.Style>
                                </Rectangle>
                                <TextBlock Grid.Column="1" Grid.Row="0"
                                           Text="{x:Static localization:Strings.UnlockTheProfileFileMessage}">
                                    <TextBlock.Style>
                                        <Style TargetType="{x:Type TextBlock}"
                                               BasedOn="{StaticResource MessageTextBlock}">
                                            <Style.Triggers>
                                                <DataTrigger
                                                    Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Button}}, Path=IsMouseOver}"
                                                    Value="True">
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource MahApps.Brushes.Gray5}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Button>
                        <!-- Profile Manager error message -->
                        <TextBlock
                            Text="{Binding Source={x:Static Member=settings:ConfigurationManager.Current}, Path=ProfileManagerErrorMessage}"
                            Visibility="{Binding Source={x:Static Member=settings:ConfigurationManager.Current}, Path=ProfileManagerShowUnlock, Converter={StaticResource BooleanReverseToVisibilityCollapsedConverter}}"
                            Style="{StaticResource ResourceKey=MessageTextBlock}" />
                    </Grid>
                </Grid>
            </Grid>
        </Expander>
    </Grid>
</UserControl>